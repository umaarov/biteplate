APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# --- BitePlate ---------------------------------------------------------------
BITEPLATE_BRANCH=standard          # standard | coastal | city_centre (Abstract Factory)
BITEPLATE_CURRENCY=GBP
BITEPLATE_TAX_RATE=20
BITEPLATE_DEV_LOGIN=true           # password-less role switcher; set false in production

# --- Kafka (Observer bridge / domain events) ---------------------------------
KAFKA_ENABLED=false                # true inside the Docker stack
KAFKA_BROKERS=kafka:9092
KAFKA_TOPIC=biteplate.events
KAFKA_GROUP=biteplate-consumers

# --- Keycloak (OIDC SSO) -----------------------------------------------------
KEYCLOAK_BASE_URL=http://keycloak:8080
KEYCLOAK_REALM=biteplate
KEYCLOAK_CLIENT_ID=biteplate-pos
KEYCLOAK_CLIENT_SECRET=biteplate-pos-secret
KEYCLOAK_REDIRECT_URI=http://localhost:8000/auth/keycloak/callback

# --- OpenLDAP (staff directory) ----------------------------------------------
LDAP_CONNECTION=default
LDAP_HOST=openldap
LDAP_PORT=1389
LDAP_BASE_DN=dc=biteplate,dc=local
LDAP_USERNAME=cn=admin,dc=biteplate,dc=local
LDAP_PASSWORD=adminpassword
